# F3 report notes — development evidence only

The implemented access layer now bounds result pages without changing the selected catalogue semantics. A shared validated query object avoids differences between HTML and JSON year interpretation; deterministic title/ID order resolves tied-title pagination. Visible typed catalogue cues support disambiguation while preserving stored headings. Existing document-owned source/item inspection remains the provenance pathway.

Keep evidence classes distinct. The new baseline is142 tests/ 1,515 assertions. The revised build passes164 tests/ 2,586 assertions. The independent synthetic experiment has234 frozen cases, one repeated read,23 complete traversals and3 comparator controls. The unchanged six-source oracle supports1,159 checks across21 requests. These denominators measure selected development cases, not percentage accuracy of the catalogue. Source-to-SQL checks, SQL-to-API agreement, HTML availability and comparator controls are separately recorded. The test suite's assertions are not independent source facts.

The first F3 implementation exposed a real route-default integration defect: default format metadata was a symbol, causing valid requests to406. The 149-test failed run (30 failures/3 errors) and first source observation are retained. Correcting the default to the exact JSON string restored all existing behavior. The same six records were subsequently observed using a verified import proof, without another import. Browser observation found a separate field-height issue; a one-line CSS correction was verified in the final browser process.

Six purposively chosen development records,48 synthetic works and developer browser actions cannot establish full-corpus fidelity, broad usability, participant outcomes or general MEI compliance. The supplied public protocol supports only the two exact instrumentation explanations; the application honestly leaves other wording unexplained. Eight screenshots were seen inline and cannot be independently viewed as exported files in this ZIP. Required200%zoom remains unverified due permitted-tool limitations; narrower viewport testing is not equivalent.

F1/F2/F2-R1 historical observations remain dated evidence. The earlier migration whole-second timestamp limitation and inherited temporary-fixture cleanup limitation remain; no new upgrade experiment was appropriate because schema/mapping/importer hashes are unchanged. Do not merge old and new assertion totals, relabel historical matrices, infer whole-work coverage from source relationships or claim a public release.

F4 still needs its separately authorized final/reserved/full-corpus protocol and any ethically appropriate participant/comparator work. F5 release/setup/clean-clone/public access checks, final report/citation/figure audits and the student's video remain pending. None was performed here.
