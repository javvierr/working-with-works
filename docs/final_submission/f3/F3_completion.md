# F3 completion — partial, for review

The bounded API/interface implementation is complete and its executed development checks pass. **F3 is PARTIAL because the required actual 200% browser zoom check remains unverified.** Stop here before F4; this is not final-evaluation or release approval.

The actual repository root is `$APP_REPO` (`/Users/javier/Documents/academia/uol/final project/prototype/UoL final project`), branch main, HEAD/main `b0142488fd5609e86d93c84978134a7d849d1882`. The supplied package stayed outside it. No applicable AGENTS.md was found in the checked ancestors or relevant safe paths. The 48 package payload hashes and accepted F2/F2-R1 archive manifests were verified. The fresh 918-path baseline reconciled to accepted F2-R1 state; no later user changes were detected. No staging, commit, push, ref/index changes or original setup/CI/cleanup commands occurred.

## Delivered behavior

The existing read-only index stays an array, now defaults to 20 works and supports bounded pages, stable title/ID order, filtered totals and relative navigation. Shared HTML/API validation rejects malformed shapes/ranges; recognized API errors use the frozen safe JSON contract. Detail/source payloads retain their F1/F2/F2-R1 semantics. The interface adds truthful catalogue cues, filter guidance, two exact instrumentation explanations, useful errors/navigation and links to existing source inspection. No schema/importer/projection/model-ownership or dependency changes were made.

## Actual validation

| Evidence | Actual result |
|---|---|
| Unchanged external baseline | 142 tests / 1,515 assertions ; 0 failures/errors/skips|
| Revised full suite | 164 tests / 2,586 assertions ; 0 failures/errors/skips|
| Independent synthetic API | 234 frozen cases + 1 repeat; 23 complete traversals; 3 deliberate wrong-expectation controls;PASS|
| Six unchanged development records | 1,159 checks / 21 requests; 25 source descriptions, 22 held items, 24 relations;PASS|
| Actual browser | 16 passed observations; required 200%zoom NOT_RUN; 8 actual inline captures, 0 exported image files|
| Database GET preservation |Complete 15-domain-table/15-sequence/schema snapshots unchanged; wrong identity controls refused before write callbacks|
| Repository preservation |PASS; final comparison and exact patch are supplied in external review evidence|

The suite/API/source build is `f3-de54529de1564a9d`. Final browser build `f3-1602645a261ed754` differs only by one CSS declaration correcting browser-observed uneven input heights. The fresh browser process confirmed all 7 input heights 40.390625 CSS px; the suite was not rerun for this style-only change. Both versions and failed attempts are retained.

## Runtime and remaining check

Only new PostgreSQL 14.23 instance `/private/tmp/www_f3_pg_2oxzep44` was used: data `/private/tmp/www_f3_pg_2oxzep44/data`, private 0700 socket `/private/tmp/www_f3_pg_2oxzep44/socket`, port 55435, bootstrap role javier, PID 69352, TCP disabled, peer authentication. Five fresh purpose-specific databases were verified absent/empty before schema writes. PostgreSQL stopped at `2026-09-20T23:00:53.903718+00:00`. Both new loopback browser processes (85975/51509 and90069/51618) stopped; all data/evidence remain retained.

The permitted IAB controls expose viewport sizing but no zoom. A browser zoom shortcut did not change the scale; native Codex access was blocked by the computer-use tool and was not retried or bypassed. Narrow 390px observations are not substituted for 200%zoom. This single required validation item remains outstanding. The ZIP integrity validation is separate from this partial acceptance outcome.

Review ZIP: `$F3_RUN/Working_with_Works_F3_Review.zip`, with adjacent `.validation.json`. It contains the F3-only patch against actual F2-R1 preimages, exact intended files, frozen contracts, observations, retained failures and preservation/shutdown evidence. See `F3_checks.json` and `F3_acceptance_results.csv`. No F4/F5 work began.
