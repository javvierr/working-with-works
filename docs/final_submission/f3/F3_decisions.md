# F3 decisions

1. Freeze before production changes. The 234-case API contract,48-row synthetic truth table and page boundaries were frozen at 2026-09-20T22:40:22.948827Z after a fresh unchanged 142/1515 baseline. Expectations were not derived from the new query or serializers and were not changed to fit responses.
2. Share decisions, preserve meaning. `WorksQuery` validates raw scalar shapes before coercion, trims outer whitespace, normalizes positive decimal input/leading zeros, combines the existing Work scopes and applies title ASC,id ASC. Work/importer/schema code remains unchanged. Input validation is independent of SQL filter implementation; literal SQL wildcards stay escaped by the existing scopes.
3. Preserve payload compatibility while bounding indexes. `/api/works` remains an array with identical summary fields; existing detail fields including both source representations remain. Clients requiring all works must follow relative page links. Fixed-data stability is not a concurrent-write snapshot promise.
4. Keep error handling at the specific boundary. API BaseController checks known format/Accept conditions and raw query parsing before Rails instrumentation can emit an HTML parameter error. Only ActionController::BadRequest from query parsing is caught; unexpected programming errors are not masked. Unknown GET API routes have a safe404 without trying to parse irrelevant parameters. Recognized endpoint order is format,query,detail ID. Period suffixes are framework formats, so `/api/works/1.5` is406; captured malformed IDs are404.
5. Retain framework parsing, validate quality. Repeated scalar query keys use the observed last-value behavior; scalar-to-container conflicts use the existing Rails parser. Rack quality parsing retains zero/out-of-range values but defaults malformed/negative q to1; bounded explicit quality validation prevents a negative JSON quality from becoming an implicit acceptance. Highest matching specificity precedes quality; the frozen negative-quality expectation remains unchanged. Framework findings and their failed probe attempts are preserved.
6. Preserve historical assertions. Only one existing HTML assertion changes: F1 instrumentation raw text is now checked in ordered `.instrumentation-source` spans because explanations are adjacent. Its original raw values and API assertion are preserved. No ownership, absence, source fidelity or rollback assertion is weakened.
7. Explain narrowly. Catalogue family comes from retained typed identity or supported document metadata, never a bare number. Only exact `1 pf.`/`1 vl.` gain explanations from the supplied public protocol. Raw wording is always visible; unknown/compound values remain explicitly unexplained and are not search aliases. F2-R1 source partials are byte-identical.
8. Isolate and retain. Fresh baseline, revised-suite, synthetic-API, six-source and browser databases belong only to a new private instance. Verified import proofs permit read-only observation continuations after an application defect; no records are silently re-imported or deleted. The second browser process verifies the first process's stopped status and exact full database snapshot before reading the same run-owned records.
9. Report limits. The final one-line CSS fix was verified in a newly started browser process because the first process retained cached CSS. Its change is separate from the 164/2586 suite build. Actual200%zoom could not be exercised through permitted controls and remains NOT_RUN. Three new root-path origins use qualified baseline Git inference; this is disclosed separately from complete preservation of 918 protected paths. No F4/F5 inference follows from these development results.
