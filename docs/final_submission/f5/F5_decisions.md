# F5 decisions and failed attempts

1. Preserve accepted unstaged/untracked bytes, not old HEAD. All execution copies are external positive selections. The supplied F5 package and F4 review/supplement remain unchanged. Original17expected API detail objects restored by the supplement stay selected response evidence, not database dumps.
2. README and portable launchers require explicit development/test targets. Setup checks installed locked dependencies, runs only migrations against an existing selected database and exits. CI sets test URL/environment/one worker on every child, includes only available focused/full tests, and propagates failure. bin/dev already forwards manual server arguments correctly and is unchanged. No dependency/schema/model/UI/setup framework expansion.
3. Rails' test helper may implicitly rebuild a database when schema_sha1 is absent. The first guarded suite's preparation child was refused before its maintenance-database connection. Setup now compares a live in-memory schema dump with unchanged db/schema.rb before writing Rails' readiness digest. A strict first comparison refused one equivalent PostgreSQL text-array cast representation. Read-only SQL proved identical arrays/predicates; only that complete unbounded varchar-literal-array representation is normalized. Six meaningful non-equivalent variants remain refused. No schema file/migration change or blind readiness stamp.
4. Initial test instrumentation blocked Rails' empty-semicolon connection-health query inside an aborted transaction, masking expected constraint exceptions and causing15failures. The external guard now permits only the empty health check on an already verified connection; rollback remains possible. An independent transaction diagnostic and the complete164test suites passed after the correction. This was an external observer defect, not an application correction.
5. A URI spelling accepted by Ruby's newer parser was rejected by Rails' RFC2396 parser before SQL. Portable validation now uses Rails-compatible parsing and rejects encoded query keys. Failed candidates and focused failing regressions remain. Initial PostgreSQL initialization/socket sandbox denials were retained and followed by scoped permission requests; no old instance/default target was substituted.
6. A source observer initially required identifier type alone instead of the approved type-or-label policy. It was corrected before the oracle freeze and any application observation; original attempt retained. The first demo HTTP observer assumed a detail-only source_identifier in the index. It was corrected to the unchanged summary/detail contract without changing expected facts or application code; failed result and closed-resource evidence retained.
7. Repeat-import comparisons explicitly permit document success/update timestamps and appended attempt logs. Rows/IDs, other values and schema remain stable. Second-copy comparisons normalize only named timestamps and candidate-root provenance; a new Rails schema readiness metadata row is reported separately. Complete row snapshots remain local-only; sanitized comparisons and selected API bodies are packaged.
8. One recursive text search violated the prefiltered-read boundary. No values were output or fingerprints made, but actual excluded-file opens are unknown. P5-01 is FAIL and overallF5 PARTIAL. Early focused-test attempts also loaded boot from the repository and may have generated ignored Bootsnap cache files; those caches were not read, hashed, moved or deleted. No attribution of individual cache additions is claimed. Preserve this limitation alongside file-byte/Git evidence.
9. F4/F3-Z1 UI/zoom evidence is historical because all relevant code is unchanged. No new participant work or usability claim. Reused installed dependencies mean this is local candidate reproduction, not cold installation or public clone verification. Publication and coursework completion remain separate human decisions.
