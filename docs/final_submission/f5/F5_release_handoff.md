# Local publication handoff — review required

**PUBLIC_RELEASE=PENDING_USER_COMMIT_AND_PUBLICATION**  
**PUBLIC_FRESH_CLONE=NOT_RUN**

The concrete positive proposal contains 255paths with complete hashes/modes in `release/candidate_manifest.json`, verified against the separately retained local candidate tree. Exact F5-only changes against actual starting bytes are in `preservation/final/patches/F5_only.patch`, with preimages and final source files. This is a local uncommitted proposal, not a public repository or clean public-clone test. No Git remote is configured; no URL is invented. Current working changes include accepted earlierF1–F4 work beyond the narrow F5 patch and must be reviewed as a whole for a future commit.

Before publication, the user should review the 255-path inventory, exactchanges and P5-01read-scope incident; check attribution/public evidence and confirm which files belong in a public release. The proposed tree intentionally excludes originalofficialXML/archive, dependency trees, whole DB snapshots, .git, retained runtime copies, secrets/private forms and unrelated local work. Exclusion is selection only; no original files were deleted. Do not stage the entire current directory indiscriminately.

After that review, a separate user-authorized step can choose the public destination, explicitly stage the intended complete tree, commit it, record its exact commit/build identity and publish. The later verification must access that exactpublic commit without authentication, clone into a new directory, provision declared prerequisites and fresh databases, run documentedsetup/demo/tests/CI, verify identities and public availability, and retain commands/exits. Installed-dependency reuse here does not demonstrate a cold installation. No publication, commit or push has been done by this invocation.

The technical phase is **PARTIAL** because the explicit read boundary was violated, even though the functionalrelease checks passed. File-byte/Git preservation and artifactintegrity are separately reported. All new services are stopped and new data/evidence retained; do not restart retainedinstances without new authorization.

Remaining human/coursework tasks: complete and check the final report, figures/citations/word counts, confirm portal cutoff and timezone, make the student's own compliant3–5 minute video, verify any final public repository links, submit the required materials and retain a receipt. This invocation did not write the final report, produce narration/video assets, assess coursecompliance or submit coursework.
