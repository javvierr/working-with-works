# F5-R1 local release handoff

**PUBLIC_RELEASE=PENDING_USER_COMMIT_AND_PUBLICATION**  
**PUBLIC_FRESH_CLONE=NOT_RUN**

The new local 264-file proposal preserves the original 255 selected paths and adds nine F5-R1 documents. Three existing runtime paths change; no new runtime files, dependencies or application features are added. Runtime `f5r1-667192fea216a940` is distinct from F5 `f5-e152006d650d0a93`; the old F5 release proposal `f5-release-08a7c33d997e720a` remains historical. The full new release identity is in `release/candidate_manifest.json`; its tree is retained outside the application repository.

Review the exact F5R1-only patch and positive inventory before any future staging. The complete current project also includes accepted unstaged/untracked work from earlier phases, so the incremental patch alone is not a public release. Selected originals and excluded corpus, private files, dependency/runtime directories and old evidence remain in place. No Git remote exists and no public URL is invented.

A later separately authorized step can approve the full intended file set and public destination, stage and commit it, then publish. Subsequent unauthenticated verification must use that exact public commit and fresh local prerequisites/targets to run the documented workflow. No publication, public fresh clone, final report/video or coursework submission occurred in this invocation.

All new services are stopped with data/evidence retained. Participant follow-up remains **NOT_EVALUATED_BY_USER_DECISION**. Stop for review; this correction does not erase F5's historical P5-01 FAIL.
