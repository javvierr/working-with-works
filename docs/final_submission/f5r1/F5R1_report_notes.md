# F5-R1 report notes

These are additive evidence notes, not a final report.

Two offline counterexamples showed that the original release helper's general isolation claim exceeded its successful runtime evidence. The correction protects every applicable declaration, requires distinct decoded database names and rejects encoded authority hosts while preserving supported sockets/IPv6. This is a deliberately conservative input contract, not live endpoint equivalence or discovery.

Report 131 prospective offline cases, 19 focused tests / 2,462 assertions and the one actual 164-test / 2,586-assertion application suite as separate measures. Development state and sentinels remained identical across CI and repeat setup. Reused installed dependencies limit the result to the tested local configuration; neither cold installation nor public clone was evaluated.

Do not relabel original F5 P5-01 as PASS. Its read-scope procedure remains NONCOMPLIANT; exact past excluded-file opens are UNKNOWN, and the supplied review records no value disclosure. F5-R1's explicit safe inventories and preservation result apply only to the new work.

The application/importer/API/UI/schema/source policies remain unchanged, so prior F4 semantic findings, 34 historical omissions, one nested-item exclusion, one unresolved relation and purposive sampling limits remain visible. Timing conditions and developer-only UI/zoom evidence remain historical. No new official import, browser check, participant session or performance campaign was performed.

Follow-up participant evaluation remains **NOT_EVALUATED_BY_USER_DECISION**. Public release remains pending user commit/publication, public fresh-clone verification is NOT_RUN, and human report/video/submission work remains separate.
