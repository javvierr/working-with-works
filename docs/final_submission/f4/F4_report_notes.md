# F4 report notes — evidence and limits, not the final report

F4 ran on 20 September 2026 locally and 21 September UTC. Preserve F0–F3-Z1 results as their original rounds; do not pool denominators or backdate evaluation to the delivery plan. All 446 inputs are accounted for. Independent source-fidelity checks and SQL/API agreement support different claims.


These are fresh F4 observations of accepted application build `f3-1602645a261ed754` (154 application files), following a source-only prospective freeze. No application correction was needed for these checks. Source fidelity, agreement between SQL and API, structural accounting and historical continuity remain different findings.

The ten-record extension passed all 1,800 frozen units independently against SQL and against captured API detail. A unit is a selected node/value object, an explicit identity/state envelope, or an array-length check; it is not a statistically independent fact. Every selected node unit compares its complete approved fields, including text/raw text, order, type/language, XML ID, attributes, namespace locator, owner metadata and nested context. A deliberately wrong expected source title failed the same comparator against unchanged SQL and API observations. Exact evidence: `validation/semantic_extension_01/semantic_summary.json`, `validation/semantic_extension_01/semantic_unit_results.json`, `validation/semantic_extension_01/semantic_negative_control.json` and `validation/semantic_extension_01/semantic_extension_sql_selected.json`. The exact API bodies are `validation/semantic_api_bodies.json`, with envelope-only extraction evidence in `validation/semantic_api_body_adapter.json` and original request references in `validation/api_corpus_detail_bodies.json`.

| Stratum | Records | Frozen units per SQL/API comparison | Work titles | Direct-work terms | Sources | Items | Source relations |
|---|---:|---:|---:|---:|---:|---:|---:|
| Six development regressions | 6 | 726 | 21 | 16 | 25 | 22 | 24 |
| Four reserved purposive records | 4 | 1,074 | 11 | 8 | 41 | 32 | 42 |
| Total | 10 | 1,800 | 32 | 24 | 66 | 54 | 66 |

Domain-level eligible/passed counts remain in the machine-readable summary. Every domain unit passed in both strata; no selected unit was removed from the frozen denominator. The four reserved cases were excluded from the previous 15-record sample and six development records, but had prior structural/identity exposure. Detailed F4 source inspection occurred before application imports. This is purposive structural selection, not a random sample or an estimate of whole-catalogue accuracy. The six development projections exactly reconciled with the prior frozen F2 source oracle and F1 titles/heading/genre facts before any F4 outcomes were observed.

The independent corpus census and SQL reconciliation passed 3,133 checks for the first import and another 3,133 for the fresh-copy replay. All 446 pinned files were represented as 446 documents and 446 works, with individually attributable success attempts for first import, unchanged repeat and replay. The separate final table is `validation/semantic_report_tables_01/F4_corpus_results.csv`; its source/accounting summary is `validation/semantic_report_tables_01/corpus_table_summary.json`. Each row carries its filename/hash/size, logical and committed identities, profile, each round's attempt/status/warnings, and exact structural/projection counts and state. `validation/semantic_census_01/census_checks.json` and `validation/semantic_census_01/census_sql_observations.json` retain independently joined ownership observations. Full source XML and full database snapshots remain retained locally and are not review-ZIP payloads.

| Domain/state | Fresh result |
|---|---:|
| Structural manifestation nodes / represented source descriptions | 2,148 / 2,148 |
| Source state: descriptive / label-or-link stub | 2,105 / 43 |
| Structural item nodes / eligible direct item nodes / represented items | 1,370 / 1,369 / 1,369 |
| Represented item state: descriptive / empty placeholder | 1,367 / 2 |
| Structural expression nodes | 1,195 |
| Direct source relation nodes/tokens / represented relations | 1,590 / 1,590 |
| Relations resolved to expression / source / unresolved | 1,499 / 90 / 1 |
| Document state: complete / absent in source / partial | 441 / 4 / 1 |

All eligible direct source and item nodes have supported IDs: `unsupported_sources` and `unsupported_items` are zero. The one structural item excluded by the selection rule is **not a missing-ID item**. CNW 2 contains `item_e5f8b4a6` at `/m:mei[1]/m:meiHead[1]/m:manifestationList[1]/m:manifestation[29]/m:itemList[1]/m:item[2]/m:componentList[1]/m:item[1]`. It is nested below another item/componentList, outside the approved direct itemList/item path; its text is XML-whitespace blank. Thus historical 2-B04 retains its original 38-node expectation and remains **omitted**, while the selected projection accurately contains 37 direct item nodes. Zero unsupported direct items does not claim that every possible nested MEI item is mapped.

The unresolved relation is in CNW 375 (`cnw0377.xml`): `relation_f777b262`, at manifestation[5]/relationList[1]/relation[1], reports `missing_fragment`. That record remains partial; the unresolved link is neither dropped nor resolved by guesswork. The corpus comparison confirms no source/item orphan, no cross-document owner/target, exact selected node identities/orders/states/counts and explicit expression/source target types. A document membership or expression target does not establish complete-work or inferred numbered-component coverage. F0's structural counts are reproduced here, but its syntactic own-payload measure is different from F2's selected-field state; descriptive, link-only and empty counts are not interchangeable.

The current historical matrix separately reevaluates all 83 original assertion IDs. The original baseline and post-correction files remain byte-identical history. Fresh classifications are **38 preserved, six transformed as intended, 34 omitted and five not applicable**; there are zero represented contradictions in this round. Original semantic eligibility stays **78** (83 minus five technical-only assertions). Current full supported-scope eligibility is **44**, and the other historical assertions remain visible rather than disappearing from the original denominator. These numbers are not an overall catalogue-accuracy percentage. All 83 selected SQL/API observations agree. Evidence: `validation/semantic_historical_02/historical_assertion_results.csv`, `validation/semantic_historical_02/historical_assertion_observations.json` and `validation/semantic_historical_02/historical_summary.json`.

Historical **277-04** is evaluated against its original first-untyped heading **Serenade**, the original ordered five titles and their language/type metadata. It is preserved on those actual comparisons. Its policy label is not rewritten to the F1 ranking. Separate `S4-277` work-display units independently check `main_untyped_uniform_alternative_subordinate_v1`, its selected source row and flags; that approved policy currently yields the same literal. The distinction remains explicit even though the two policies agree for this input.

Remaining historical omissions cover the full expression/component hierarchy, additional contributor roles, rich/occurrence-bearing performance-resource metadata, broader work/component/graphic target graphs, the empty event case and the nested-item boundary. Selected source-to-expression context does not erase these wider omissions. Exact ranged/uncertain date wording is retained with the declared scalar year; selected event values retain their meaning through existing flat strings. Broader event bibliography/whole XML is not claimed by that transformation.

The full unchanged repeat passed 50 independent checks: all non-log domain identities, associations and nonexcluded values remained exact; existing 446 logs stayed unchanged and exactly 446 new successful logs covered the documents once each. Only `catalogue_documents.last_successful_at` and `updated_at` actually changed for all 446 documents. `latest_successful_path` was permitted but remained unchanged. Timestamps were observed at six fractional digits. Non-log sequences and structural schema were unchanged. Evidence: `validation/semantic_reimport_01/reimport_summary.json`, `validation/semantic_reimport_01/reimport_checks.json` and `validation/semantic_reimport_01/reimport_allowed_field_observations.json`.

The fresh-copy reproduction passed 49 independent checks over all 17,217 first-import domain rows. Values, primary/foreign identities, input paths/hashes, log outcomes, sequences and structural schema matched. Fresh created/updated timestamps, import-log imported_at and document last_successful_at were the declared temporal exclusions; raw timestamp differences are retained locally. The corresponding new-schema Rails metadata timestamps are likewise temporal creation facts, while environment/checksum and structural schema remain exact. Source census replay also passed 3,133 checks. This is pre-release selective external-copy reproduction, not public committed-clone/setup/CI verification; those remain F5.

Two observer defects were retained and corrected without changing frozen source meaning, application/data or captured responses. `validation/semantic_historical_01/` preserves the first count-only 17-B06 observer's accidental extra first-event comparison; version two limits the observed fields to the original `{events: 211}` assertion. `validation/semantic_reproduction_01/` preserves the initial omission of fresh Rails metadata timestamps from the already declared temporal exclusions. An earlier prospective public-shape adapter correction is retained in `acceptance/semantic_frozen/semantic_adapter_revision_v2.json`: the accepted API uses display_title_policy.name and selected_for_display flags. It was identified from historical API-shape evidence before F4 outcomes; XML values and source expectations stayed fixed. None of these corrections relaxed a source expectation to obtain a pass.

The source-oracle freeze is `acceptance/semantic_frozen/semantic_freeze_manifest_v1.json`; the final 1,800-unit public-shape freeze is `acceptance/semantic_frozen/semantic_adapter_freeze_manifest_v2.json`. The first source-only preparation remains under `acceptance/semantic_attempt01/`. All scripts compare captured facts offline using their own explicit joins and a separate namespace-aware source reader; they call no application importer/filter/parser/serializer to generate expected meaning.

`FOLLOWUP_PARTICIPANT_EVALUATION = NOT_EVALUATED_BY_USER_DECISION`. These technical findings and the separate developer walkthrough do not prove that earlier formative participants' difficulties are resolved, establish expert validation or provide a new usability success rate. FB01/FB10 domain/literature/comparator reasoning remains open; FB08/O5 participant evidence remains unevaluated. Report/source limits and public-release gaps must remain explicit. F4 stops for review before F5, staging, commit or push.

## Operational, participant and release limits

Operational observations comprise 150 measured warm-cache, in-process Rack requests plus 25 warm-ups, executed serially with live identity-guard overhead. They exclude HTTP transport and browser rendering. They are not a load benchmark, speed-threshold result or evidence of comparative superiority. The optional hardware query was denied; precise hardware metadata remains unavailable. Parser and transaction cases use bounded benign synthetic inputs and do not establish security certification. Browser observations are developer inspection, not accessibility-conformance or participant-study findings.

No recruitment, consent/session materials or private participant records were created or accessed. The prior public three-person/nine-attempt formative study explains motivation, not success on the new build. Follow-up participant evaluation under O5 was not performed by explicit user decision; this does not block technical F4 completion. FB01 domain need/fair-comparator evidence and FB10 primary-literature claims remain open.

O2 is supported by complete input/reimport/failure-integrity accounting; O3 by selected source fidelity with declared omissions; and O4 by independent API comparisons and developer observations without a usability claim. O1 still needs domain/comparative evidence. O6 has pre-release external-copy reproduction only. Final committed public-clone/access/setup/README/CI checks, the final report and figures, word-count audit and student video remain F5 or later work. No overall submission-readiness claim follows from F4.
