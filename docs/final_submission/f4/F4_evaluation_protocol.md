# F4 evaluation protocol — frozen before final corpus imports

Candidate: accepted f3-1602645a261ed754, exactly154allowlisted files. Actual starting HEAD/main b0142488fd5609e86d93c84978134a7d849d1882; accepted945safe paths preserved, current unstaged/untracked work retained. See preservation/attempt03 and builds. This run began21September2026UTC (20September local), independently of planned calendar dates. Package manifest and exact archive/446XML identities verified before source parsing.

Authority is the supplied F4 contract and latest invocation only. No production change is planned. FOLLOWUP_PARTICIPANT_EVALUATION = NOT_EVALUATED_BY_USER_DECISION. Structured Codex-led developer checks are distinct from the historical public three-person/nine-attempt formative study; no new sessions, private records or participant materials.

## Prospective stages and exact evidence

1. Fresh safe baseline/inventory, package and corpus identities, then two exact154file external copies. New peer-only PostgreSQL14.23, TCP disabled, private0700socket/data under owned www_f4_pg_pkop14vj, port55437. Six verified-absent target names are in targets/*.json. The F4 guard checks configuration and live identity inside every schema/test/import/API process. No setup/CI/drop/reset commands.
2. Freeze source-only full446structural/ownership census, ten-record semantic values and historical83assertion definitions, API selection/query/comparison rules and timing rules. Hash manifests in acceptance/. Source v1 remains immutable; adapterv2 corrects public key pointers using accepted historical contract before F4outcomes; units remain1800. Preserve all earlier preparation attempts. Independent expectations never call production importer/parser/filter/serializer.
3. Run unchanged suite in isolated suite target. Run fresh234frozenF3cases against unchanged48truthfixture on synthetic_api target. These baseline outcomes do not inform semantic expectedvalues.
4. Canonical corpus target: schema then exactly446official inputs; capture everyattempt, document/work/result/count/state, complete relevant SQLtables/sequences/schema, typed-target/ownership integrityqueries. Importfails remain visible. Compare against sourceonly census and tenrecordvalues, not historic successcounts.
5. Whole same-byte corpus reimport. Exact14domain tables (all except import_logs) mustretainrowIDs/values/associations. Only catalogue_documents.last_successful_at,updated_at,latest_successful_path are excluded from semantic equality because these are successful-attempt provenance; samepath is separately recorded. Import_logs mustaddone attributable successperinput with unchanged_bytes true and source_projection_updated false; only its sequence mayadvance. Snapshot timestamps use6fractional digits, measured SQLcolumn precision recorded; public API uses3. Preserve fullbefore/aftersnapshots locally (notZIPdatabasepayloads), include counts/hashes/comparison.
6. Materialize independent SQL API expectations and hashes after imports before requests, using frozen queryrules and actualCcollation. Allfilters/combinations/zero/beyond, fulltraversal20/7/100, title ties and10details. Keep sourcefidelity separate from SQL/APIagreement; capture extra historicaldetails asneeded for83continuity. Complete15domain tables/sequences/schema unchanged after GET, explicitwrite-likeevents andzeroSQLrejections.
7. Separate safetytarget: freeze bounded synthetic parser/transaction/identity/partial-item cases before execution; noexternalfetch/secretprobe. Retainfixtures, failures,lastvalidsnapshots. Sameapprovedpolicy only.
8. Timings use acceptance/api_timing_rules_v1.json: five scenarios,5warmups then30serialmeasured each; medianp50, nearest-rankceil(.95*n)p95,min/max,allstatus/correctness retained. InprocessRackGET timing includesidentityguardoverhead, no network/productionlatency claim, no pass threshold.
9. Structured developer walkthrough: find/disambiguate actual same titles using title+catalogue information; contrast q=Duet and catalogue_number=CNW417 using sourcebackedrecord417; invalid year recovery/Clear and pagination; inspect raw instruments and known/unknown labels; inspect source/item ownership and expression context on417,1,202, absent/partial limits on Coll21/approvedhistoricalsynthetic. Browsersteps on newownedloopbackChrome tab only, inspectkeyboardfocus/narrowflow. Carry accepted unchangedF3/F3Z1actual200%zoom/layout with originaldates/build; no freshzoom claim unless actually performed. Developer observations are not participant or accessibility-conformance passes.
10. Second fresh selective copy reproduction_app: new reproduction_corpus and reproduction_suite targets, same guarded schema/full446import and actual164-test suite (number is historical expectation only until run). Compare actual outcomes/independent semantic summaries. Final publicclone/setup remainsF5.
11. Deliberately wrong expected semanticvalue/API member/order/total mustfail unchangedcomparators without changingdata. If real consequential defect, preservefailingcase/build/preimage, smallestapprovedcorrection+regression, freeze revisedbuild and rerunaffectedchecks+fullsuite. No schema/policy/dependency/newfeatures. ReportPARTIAL if boundaryprevents correction; neveralteroracle tofitoutput.
12. Retainfailedattempts/allnewdata, stoponlyownedservices/closeownedtab, final945safe preservation/Gitcheck, exactF4-onlytool/docpatch, allowlistedreviewZIP+manifest+producerandindependentreadback. StopbeforeF5/staging/commit/push.

## Independent API protocol

Authoritative pre-import rules: acceptance/api_corpus_rules_v1.json, acceptance/api_timing_rules_v1.json and api_freeze_manifest_v1.json. Unchanged original234case definitions,48truth rows and complete23traversal boundaries under acceptance/api_f3_frozen/. Filter literals are source/publichistorical selected; zero sentinel is fixed before outcomes. SQL uses independent literal-substring/existence logic, ANDcombinations, title,idordering and exactnormalizedrelative links. Missing realconditions are disclosed, not manufactured. Deliberate controls alter expectedmember/order/total only. Selectedsource projections/details are allowedreviewevidence; fullcorpusXML/archive, fullruntime or database-statepayloads excludedfromZIP.

# F4 prospective semantic method

This independent method is frozen before any F4 application import/SQL/API observation. Source expectations come only from pinned selected CNW XML, approved F1/F2/F2-R1 policies and earlier independent source oracles. The reader uses Python standard-library ElementTree with explicit MEI namespace and direct ownership paths. It does not import application code, query a database, use a serializer or fetch links. The six development projections and F1 typed title/heading/genre facts reconcile exactly with the earlier frozen independent oracles. All 446 source hashes are rechecked before their bytes are parsed.

## Units and denominators

The ten-record extension uses versioned units in semantic_assertion_units_v1.json: each selected value/node is one deep-comparison unit encompassing all its approved fields, exact text, raw parsed text, order, type/language, XML ID, attributes, namespace locator and owner metadata. Each array also has a length unit so absent arrays and unexpected extra rows cannot pass. Source/item envelopes, document identity, projection summary, heading policy and genre each have explicit units. Physical-location units include their full ordered repository identifier/name/link arrays and separate location identifiers; relation units include the full ordered expression title/ancestor context or explicit same-document source target. Compound unit counts are not counts of statistically independent facts or catalogue accuracy. Results distinguish the six development and four reserved strata and each domain. SQL-to-API agreement and source-to-SQL/source-to-API fidelity are reported separately.

Work titles retain only nonblank direct Work/title rows, ordinal positions among all direct titles, exact raw type/language/attributes. Heading ranks main, untyped, uniform, alternative, subordinate; XML-whitespace blank type is untyped, unknown exact types cannot be headings, no language preference. Direct Work/classification terms are nonblank retained terms ordered across direct termList chains; empty terms are omitted and do not advance term order. Nested bibliographic/work ownership is excluded, and genre is the first retained direct work term. Source and item selected value arrays retain blank nodes with null normalized text and raw parsed character content. Normalization collapses U+0020, tab, CR and LF only; attributes remain exact Clark-named strings. No spelling, punctuation, authority, language or shelfmark convention is inferred.

Source descriptions belong to documents. Held items belong to a particular represented source and same document. Missing IDs are unsupported, not fabricated identities. Own selected non-link text determines descriptive state; label/link-only and empty placeholders remain distinct. Child item text does not make a source descriptive. Empty source arrays mean no direct source nodes only when the summary confirms absence; unsupported/partial and not-projected remain distinct. Counters include unsupported direct child nodes, and bounded issues never substitute for exact counters.

Only exact isEmbodimentOf targets to a same-document expression in the supported work resolve as expression contexts. Only exact isReproductionOf/hasReproduction targets to a represented same-document direct source resolve as source edges. Retain target direction and nested expression context, never infer whole-work/component coverage from source titles. Missing/unsupported target forms and missing/wrong target identities use approved explicit precedence; no cross-file/network/URI-decoding resolution.

The full-corpus census independently records every pinned input, structural manifestation/item/expression/direct relation counts, selected representation state/counts, source/item identities and ownership, target tokens and typed resolutions; nonblank/blank selected source-value counts accompany sources. F0 structural totals are historical comparison only. Selected-field state is narrower than F0's syntactic own-payload rule; semantic counts need not equal that historical measure. F1 titles/classifications are exhaustively compared for the ten extension records, not claimed exhaustive semantic verification of every title in the 446-file corpus.

## Historical 83 assertions

semantic_historical_cases_v1.json preserves every original ID, expected value, mapped meaning, domain, eligibility and both old classifications; current observations are separate. Original 83 rows remain the historical denominator; technical_import rows are not semantic eligible. Original eligible rows remain visible even when their broad domain is unsupported. Source facts retain namespace-aware locators, direct contributors, identifiers, full title metadata, dates, expression structure, performance resources/events and target categories. Their comparison evaluates the original compound expectation: preserved if all stated meaning survives, transformed_as_intended only when the declared legacy normalization preserves the asserted meaning, omitted when any noncontradictory part is missing, incorrect when represented meaning contradicts the assertion, not_applicable for original technical-only units. Supported-scope eligibility is a separate field, never a reason to erase historical rows. Unsupported expression/component hierarchy, contributor roles beyond composer, structured performance-resource attributes, event richness and work-target graph/category limitations remain disclosed.

Legacy 277-04 retains its original first-untyped heading comparison (Serenade), ordered five titles, types and languages. Independently evaluate the approved F1 heading ranking as the separate S4-277 work_display units even if both policies currently select the same literal. Do not relabel the legacy assertion as a pass under the new policy.

## Comparison and controls

Comparison uses strict recursive equality: array order/length exact; object key order irrelevant; null differs from missing; generated SQL primary keys are resolved only by explicit same-document ownership joins. SQL storage source_attributes maps only to the public attributes field; no production serializer/helper establishes expectations. Compare source-to-SQL and source-to-API independently, retaining all mismatch paths and snapshots. Historical broader-domain omissions are explicit limits and are not counted as passes. After genuine observations, change one expected source title value only and run the same comparator against unchanged captured SQL/API data; it must fail and must leave inputs/application/data untouched. Preserve earlier oracle versions; any independently justified change creates a new version with reason before rerunning affected comparisons.

This is technical/source evaluation of a purposive selection, not domain-expert validation, a new participant study or overall catalogue accuracy. FOLLOWUP_PARTICIPANT_EVALUATION = NOT_EVALUATED_BY_USER_DECISION.
