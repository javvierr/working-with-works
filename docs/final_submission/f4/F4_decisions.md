# F4 decisions and retained dispositions

The accepted application required no consequential production correction. Additions comprise evaluation tools and documents. Independent expectations preceded outcomes: the master protocol froze at 00:52:13.459585 UTC, before the first import at 00:52:30.918259 UTC. Independent SQL-derived API IDs, order and counts froze at 00:57:21.209132 UTC, before the first canonical request.

The semantic adapter's second version corrected public field pointers (`display_title_policy.name` and selected-title flags) using accepted historical contract evidence before F4 import. The source oracle remained unchanged. Enumeration of the four additional historical detail records at 00:52:55 followed import start, but only listed identities already selected by the frozen 83 cases and master protocol. The 109 canonical error cases likewise apply unchanged frozen contract templates. Their actual timestamps and dispositions remain recorded; no later enumeration is claimed to have preceded import.

The independent census found 2,148 manifestations, 1,370 structural items, 1,195 expressions and 1,590 direct source relations, matching F0's structural history. The approved direct `itemList/item` path represents 1,369 items. CNW 2's nested `item_e5f8b4a6` lies below an item/componentList despite having a valid XML ID. It is outside the selected path, **not a missing-ID item**; corpus `unsupported_items` is zero. CNW 375 (`cnw0377.xml`) contains one unresolved `missing_fragment` relation and truthfully remains partial. Source states are 2,105 descriptive and 43 label-or-link stubs; item states are 1,367 descriptive and two empty placeholders. Relation resolutions comprise 1,499 expressions, 90 sources and one unresolved target under the approved document-local policy.

For unchanged reimport, semantic equality excludes only document `last_successful_at`, `updated_at` and `latest_successful_path`. The two timestamps changed for all 446 documents; the path remained unchanged. Existing 446 attempts stayed unchanged, exactly 446 successful attempts were appended, and only the import-log sequence advanced. Fresh-copy reproduction excludes legitimate creation/update/import/provenance timestamps, including Rails internal metadata timestamps; domain values, IDs and associations remain compared. Captured SQL timestamps had six fractional digits; public JSON follows its existing three-digit timestamp representation.

Validation-tool failures remain distinct from application failures:

- Preservation attempt 1 used an overly strict extra-name filter; attempt 2 mishandled a leading space in porcelain status. Neither mutated files. Attempt 3 reconciled the 945 accepted safe paths; four unchanged tracked exclusions are listed by name only.
- Runtime preflight caught stale prior-run literals and a fixture label before execution. The revised guard uses exact F4 targets and copies; the frozen fixture label was restored.
- First suite/schema attempts supplied a URL query key that did not configure Rails' username. The identity guard refused before SQL. The external username key was corrected, and the same targets were loaded only after the guard passed.
- The optional hardware query was denied. OS, architecture and runtime were recorded; precise hardware metadata remains unavailable. No retry or workaround followed.
- Safety cases SAF25 and SAF33 encountered SQL-JSON observation decoding errors after their actual imports and domain-equality checks. Six remaining assertions were completed offline from unchanged captured logs. Original failures remain retained. These two cases lack full per-case after-snapshots; the completed semantic equality checks and attempt observations are retained.
- The initial historical 17-B06 observer compared a first event even though the frozen assertion requested only an event count. The failure was retained and observation selection restored to the original unit. The initial reproduction comparator omitted fresh Rails metadata timestamps from the declared temporal exclusions; its failure and correction also remain visible. No source meaning or application changed.
- A root offline CLI generated an owned `__pycache__` directory under the new F4 tools. It was moved intact, without reading, hashing or deleting its contents, to the external run's `retained_generated_cache/` directory. The launcher now uses `-B` and passed a rerun with scratch kept outside the repository. No historical file was touched.
- Maintained comparison tools now propagate a logical failure as a nonzero exit. A deliberate expected-only source-title mutation produced one SQL and one API mismatch and exit 1 against unchanged observations. The historical gate also rejects contradictions or omissions within currently supported scope. Earlier command exits and logical failures remain recorded as actually observed.
- A browser button-role lookup did not match native SUMMARY. Fresh accessibility/DOM evidence supplied the documented summary selector, and keyboard activation worked. An attempt to empty search left `q=Duet`; that combined result was retained, followed by Clear and a separate catalogue-only check.

No omitted historical domain became a pass by changing its meaning. Legacy 277-04 retains its first-untyped **Serenade** rule and all five title records; the current build satisfies that comparison. The separately approved F1 ranking policy also passes. Wider expression/component graphs, additional responsibility roles, resource attributes and the nested-item domain remain limitations. Technical tests and a developer walkthrough cannot close domain/literature/comparator feedback FB01/FB10 or establish participant benefit under FB08/O5.
