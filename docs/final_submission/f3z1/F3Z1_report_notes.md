# F3-Z1 report notes

The outstanding F3 developer zoom observation is now supported on the exact final F3 build. The user supplied the fallback action (reported prior 100%, now 200%); direct Chrome toolbar/dialog accessibility then independently established 200%. After the required flow, the tool restored and directly observed 100%. This supports proposed V3-01 closure for review. Do not rewrite the historical F3 PARTIAL result or describe this as a new execution of its accepted full suite, API experiment or six-record regression.

At 200%, required labels, errors/recovery, same-title catalogue cues, bounded raw-preserving explanations and filtered keyboard navigation remained usable. Dense CNW 17 source/relationship/item inspection worked by native keyboard disclosures and normal scrolling. The missing-ID partial fixture and Coll. 27 genuine absence remained distinct. No loss of essential content or controls, overlap or horizontal scrolling was observed; some narrow table fields wrapped digits/words. That bounded observation is neither a participant result nor a WCAG/conformance claim.

The evidence separates direct Chrome AX zoom labels, actual inline page captures, structured developer observations, request/SQL guards, full GET-state comparisons and safe repository/Git measurements. Fifteen inline captures are real; no screenshot file export is claimed. A failed native capture and unavailable version inspection are retained. Two immediate metadata URL samples lagged navigation; original samples are preserved with subsequent fresh AX/server corroboration.

Only a new authorized instance/database/external copy and owned local Chrome tab were used. Eleven GETs preserved all 15 domain tables, 15 sequences and structural schema. Browser and PostgreSQL processes stopped; populated data retained. Initial sandbox failures used the normal scoped permission process, without a repeated denied route. `initdb` performed its default failed-initialization partial-file cleanup before application data existed; no agent deletion occurred, and the next new initialization explicitly used --no-clean. The incidental initial unrelated native foreground state is disclosed without retaining its contents or making a whole-browser preservation claim.

All 941 protected paths, the 154-file application build and earlier F3 documents remain unchanged. Add only the four new F3-Z1 documents and evidence; the application patch is empty. The earlier three-file origin qualification remains historical and is not upgraded to direct filesystem evidence. Archive integrity and docs-only patch verification are separate from visual acceptance. F4/final corpus/participant evaluation and F5/release work remain outside this invocation. No staging, commits, pushes, refs/index changes or publication occurred.
