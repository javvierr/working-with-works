# F1 report notes — new evidence, original report unchanged

The contribution is a validated document foundation with corrected work-level classification, typed-title preservation and truthful import history. The three entities separate ingestion identity from the work projection; they do not yet model a musical source, holding or relationship. The scoped input contract and path-ownership conflict rules prevent shape guesses, filename-derived identity and accidental transfer on re-import.

The original implementation reproduced two classification errors (417 manuscript and 63 letter), loss of the preferred CNW 277 heading/variants, and a failed re-import that left older success displayed. Prospective title/term policies and acceptance expectations were frozen before implementation. Old matrices, including historical 277-04, remain unchanged. CNW 277 now displays Serenade by the declared untyped/source-order policy, with five typed/language rows preserved. This is an explicit policy choice, not evidence that one language or catalogue title is universally preferred.

The evidence has distinct layers. P0's unchanged suite freshly produced 58 tests/269 assertions; its four-request probe separated literal source expectations from independently queried SQL transport expectations. The revised suite produced 96 tests/781 assertions and no failures/errors/skips. The source/API/HTML run passed 222 checks across six selected development records and 21 requests. SQL and source oracles did not call importer helpers, Work associations/filters or serializers. Intentional comparator mutations were rejected against unchanged responses. Request-time connection identity and state snapshots support isolation and read-only request behavior for the measured tables/sequences/schema, not universal absence of all possible external effects.

Meaningful synthetic cases cover malformed/unsupported input, explicit demo boundaries, identity collisions, same-byte relocation, version/conflict handling, duplicate titles/terms, wildcard escaping, missing classification, unknown title types, transaction failure, legacy rows and attempt ordering. A post-parent-update child persistence failure rolls back semantic rows, provenance, committed hash and parent timestamps; one associated failure remains outside rollback. Sequences can advance on rolled-back transactions and are not semantic rollback facts. Attempt history is deliberately append-only. Unknown failures do not claim a document path.

The six examples are CNW 417, 277, 63, 17, 45 and Coll. 27. A separate duplicate-aware legacy projection comparison found no change in measured existing fields/children for 417/277/63 (10 catalogue identifiers, six instruments, 38 performances and one external reference; movements/source references remain zero). The other three have approved-domain source checks and smoke coverage, not that same old/new field comparison. This does not measure full corpus fidelity, precision/recall or usability. Reserved detailed 48/1/Coll. 21/202 were not used; their earlier structural profiling exposure remains an F4 limitation. No second catalogue or full 446-record import occurred.

Report nonpassing attempts rather than erasing them: the first P0 URL failed before SQL; one revised harness passed. Subsequent implementation checks exposed a test ordering error, a probe parse error, SQL JSONB result typing and API instrument-order hydration. Their retained outputs, narrow corrections and final passes are indexed in evidence/failure_dispositions.md. Source expectations were not rewritten to fit these changes. The missing baseline detail response remains HTML 404, and original source/item extraction remains limited.

Operational reproducibility is improved by a maintained isolated runner with explicit target/creation proof, configuration/live identity checks, no implicit target or service/package/cleanup operation, a fixed seed, and schema checks before test helpers. It does not fix the original setup/CI or establish clean-clone/public access. The user authorized the disposable private PostgreSQL cluster after no existing endpoint was established; it was stopped with data retained. Existing user databases were never migrated. Command records retain actual exit codes and timestamps where available; unavailable early diagnostic start/end timestamps remain null.

The preservation comparison supports unchanged source XML, historical evidence, F0 documents and initial user work, with unchanged HEAD/index/main. Two missing Codex temporary refs are recorded without assigning a cause. Disclose the initial collector's accidental reads/hashes of two secret-named files: no values were printed/copied, the fingerprints are excluded from review and the original manifest stays private. Excluded private/secrets/tool/dependency paths prevent a total-machine preservation claim.

FB05 has implemented repairs awaiting broader evaluation and explanation in the final report. FB07 has expanded technical evidence but final evaluation/source domains remain open. FB11 has a guarded F1 entrypoint and private review artifact, while setup/CI, clean-clone and public-access work remain F5. The proposed register is a separate document; no original feedback register or final report was overwritten. F2 remains wholly outside this implementation.
