# F2 report notes — bounded evidence, 20 September 2026

F2 gives provisional objective O3 a concrete source-description/item pathway while contributing to O2 atomic transformation and O4 truthful read-only access. A source is owned by its catalogue document; an item is owned by one description in that same document. Explicit expression context is a source statement, not proof of whole-work completeness. Reproduction edges preserve direction without traversal or inherited work ownership. This distinction explains the design more usefully than the count of new tables.

CNW 63 has six descriptions and seven items. Under source_d1e231, item_d1e644632 retains DK-Kk/CNS24d while item_d1e879862 retains DK-Kk/DFM29,nr.33,6. CNW 17 retains “Score,draft(No.26)” at source_d1e136 and its actual expression_1 target; no numbered component is inferred. CNW 417 has no manifestation/item nodes; Coll. 27 has one description but no item or relation. These contrasting cases justify separate absence, membership, relationship and item concepts. Exact punctuation, locators and ordered values are in the frozen source oracle and observed SQL, not these abbreviated explanatory labels.

The six-record oracle was frozen before projection implementation using namespace-aware source inspection, independently of importer/models/serializers. Source→SQL checks compare complete selected arrays/parents/locators and structural totals25/22/24. SQL→API expectations come from direct SQL.939 visible HTML assertions are separate from21 source-to-SQL aggregate comparisons; no denominator should be presented as overall musical accuracy. The complete source/API runner has1,070 checks and21 requests. Its altered-parent control changes the expectation, not the actual response, and the same comparator detects the precise mismatch. All15 measured domain tables, sequences and schema metadata stay unchanged through GETs.

The unchanged F1 baseline passed96/781. Final build6 passed133/1,209. The mapping-upgrade experiment starts with genuine fresh F1 schema/import rather than retained F1 databases or synthetic NULL-only setup: migration leaves legacy records not_projected; identical XML adds projection without changing old Work/child identities/content; the repeat is stable.231 checks cover the migration/upgrade/repeat and36 GETs. Original before-migration timestamp precision is only whole seconds, explicitly limited; all later comparisons retain microseconds. Deliberate partial-write and real constraint failures restore previous semantics and show the failed latest attempt alongside the successful data import before recovery.

The actual browser check covered CNW 417,63,17,45 andColl. 27. Native source disclosures made the dense17 source list navigable with Enter/Tab/Space and visible focus at a narrow498px viewport. This is developer verification, not a replacement for consented participant tasks, accessibility certification, multiple-browser testing or F4 evaluation. Existing instrumentation/search/pagination/general error-contract questions remain later work.

Report historical F0/F1 matrices and failures under their original versions; do not update old277-04 or convert this development set into final held-out evaluation. Retain failed execution attempts and the temporary-test-fixture cleanup limitation. Do not infer full corpus fidelity, general MEI/FRBR compliance, concurrent safety, unmet user need, superiority to other interfaces, public-release reproducibility or final objective closure from this package. Feedback FB06 and FB07 gain bounded implementation/technical evidence but remain awaiting wider evaluation. F3 was not begun.
