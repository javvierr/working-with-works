# F2-R1 report notes

F2-R1 repairs one misleading presentation claim without changing the stored representation. A source's `held_items` array counts represented items; a real XML item without an XML identifier is counted as unsupported but omitted from that array. The earlier view called the represented count “item nodes” and inferred supplied-node absence from an empty array. The corrected view names represented counts and permits an absence claim only when the exact persisted document unsupported count is zero. Positive counts use record-wide wording, and missing counts remain uncertain.

The new C2 isolates a represented source containing exactly one missing-ID item. Its unchanged API reports partial state and 1 actual / 0 represented / 1 unsupported item. Against the old partial, the regression failed on the false absence sentence: 1 test, 16 assertions, 1 failure, exit 1. Against the corrected partial, it passed: 1 test, 34 assertions. The older extracted review probe contained two unsupported items in the record (2 / 0 / 2); do not conflate its document totals with C2.

Historical F2 S2-07 and E2-02 PASS rows remain unchanged. They accurately record assertions that passed, but those assertions missed this disclosure gap. The new F2-R1 register records the correction and C2–C7 regression coverage. C5 rejects source-level attribution from record-wide unsupported counts; C6 places the omitted item's issue beyond the first 50 retained issues, proving that the exact document count, rather than issue-list membership, determines uncertainty.

The fresh unchanged baseline passed 133 tests / 1,209 assertions. The corrected full suite passed 142 tests / 1,515 assertions, with no failures, errors or skips. All nine new focused tests are represented in the full result details; no separate nine-test-only run is claimed. The source/interface run passed 1,145 checks across 21 requests, including 75 corrected-availability HTML assertions, while retaining the same six records, frozen field/source expectations and independent SQL comparators.

Keep the evidence classes separate. Frozen source expectations check selected source fidelity; direct SQL reconstruction checks persistence and ownership; actual guarded requests check SQL/API agreement; scoped DOM assertions check HTML wording and fields; a deliberately wrong expected item parent exercises the same comparator; before/after table, sequence and schema snapshots check GET state preservation. These checks remain bounded to CNW 417, 277, 63, 17, 45 and Coll. 27. The run did not inspect detailed reserved evaluation records or perform a full-corpus import.

The independent read-only evidence audit passed 130 checks (`evidence/runtime_results_audit.json`). Source-run snapshots replace execution paths with declared `$F2R1_RUN`/`$REVISED_APP` aliases after computing the raw SQL-state hashes. The first audit attempt recomputed hashes before reversing those aliases and therefore reported a serialization mismatch. Reversing only the declared aliases reproduced the per-table and aggregate hashes without adjusting source values, arrays or counts. Preserve `evidence/runtime_results_audit_attempt_1.json` and `evidence/runtime_audit_disposition.json`. A subsequent explanatory-text edit caused a pre-execution Python syntax error, retained in `evidence/runtime_results_audit_attempt_2.json`; correcting that explanation preceded the passing audit. These were audit-method attempts, not application failures or database reruns, and are separate from the expected C2 red test.

The actual browser observations cover synthetic C2, genuine item absence in Coll. 27 and dense represented-item captions in CNW 17. Enter/Return expanded the focused first native disclosure and Space collapsed it, retaining visible focus. Three screenshots were captured and inspected inline, but no screenshot files could be exported through the exposed method. The archive contains structured observations and guarded runtime evidence, not fabricated screenshot files. This is a narrow developer check, not participant usability evaluation.

No F1-to-F2 migration experiment was rerun. Accepted prior upgrade/repeat evidence—231 checks / 36 requests—is carried forward alongside unchanged model, mapper, importer, projection-version and schema files. Preserve its precision limit: the original pre-migration snapshot used whole-second timestamps; later upgrade/repeat/GET snapshots retained microseconds. This correction's fresh GET snapshots retain microseconds and do not enlarge the older migration claim.

Fixture preservation also has a specific limit. Some earlier F2 generated copies were deleted before retention was repaired. The unchanged inherited suite may clean up its own new temporary fixtures. New F2-R1 synthetic blueprints, imported copies, the expected failed reproduction, commands, logs, stopped database data and evidence are retained. Protected original XML, earlier reports, existing retained resources and unstaged/untracked F1/F2 work are preserved. Do not claim that every generated file across earlier phases was retained.

The only production correction is the existing source-detail partial; the other new source files are focused tests and distinctly named validation tools. Final correction-only patch, Git and preservation records are in `evidence/F2R1_only.patch`, `evidence/F2R1_patch_check.json`, `evidence/git_after.json` and `evidence/preservation_comparison.json`. Both newly created services are stopped and their data/evidence retained. No staging, commit, push or F3 work occurred.

The adjacent `Working_with_Works_F2R1_Review.validation.json` is the authoritative final archive readback/allowlist/hash result. No circular ZIP size/hash is embedded in these documents. Final C10 acceptance requires that adjacent result to pass. Development-set results do not establish full-corpus accuracy, final participant usability, cross-file resolution or general MEI compliance. F3 remains pending review.
