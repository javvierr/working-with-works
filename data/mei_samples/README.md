# MEI sample fixtures

These are small MEI-like sample records for the prototype importer. They are not authoritative Carl Nielsen catalogue records and should be replaced with real Royal Danish Library / LOAR MEI files for further evaluation.

The importer reads `.xml` and `.mei` files recursively from this directory by default. Use `MEI_DIR=/path/to/mei bundle exec rails mei:import` to import another directory.
